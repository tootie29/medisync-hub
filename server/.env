
# Production Database Configuration
DB_HOST=localhost
DB_USER=entrsolu_climasysuser
DB_PASSWORD=+DrJ)ObKCc)2
DB_NAME=entrsolu_climasys

# IMPORTANT: Using port 8080 instead of 3001 to avoid conflicts
# If this port is also in use, check your cPanel interface for the correct port
PORT=8080

# Production environment settings
NODE_ENV=production

# Application URL path for subdirectory access
APPLICATION_URL=/server

# Disable database debugging in production
DB_DEBUG=false
