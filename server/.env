
# Production Database Configuration
DB_HOST=localhost
DB_USER=entrsolu_medisyncuser
DB_PASSWORD=+DrJ)ObKCc)2
DB_NAME=entrsolu_medisync

# Using a different port for the API server to avoid conflicts
PORT=8081

# Production environment settings
NODE_ENV=production

# Application URL path - set to empty for API-only server on a separate domain
# If you're hosting on a subdirectory, set this to the subdirectory path (e.g., /api)
APPLICATION_URL=

# Enable database debugging for troubleshooting
DB_DEBUG=true

# CORS configuration
# For production, allow requests from the main domain
CORS_ALLOWED_ORIGIN=*

# Upload directory - setting absolute path for production environment
# Exact path to your API domain folder
UPLOAD_BASE_DIR=/home/entrsolu/api.climasys.entrsolutions.com

# Debug level for file operations
FILE_DEBUG=true
