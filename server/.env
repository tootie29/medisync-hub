
# Production Database Configuration
DB_HOST=localhost
DB_USER=entrsolu_medisyncuser
DB_PASSWORD=+DrJ)ObKCc)2
DB_NAME=entrsolu_medisync

# Using standard port 8080 for the API server
PORT=8080

# Production environment settings
NODE_ENV=production

# Application URL path - set to empty for API-only server on a separate domain
APPLICATION_URL=

# Disable database debugging to reduce connection overhead
DB_DEBUG=false

# CORS configuration - UPDATED to explicitly allow frontend domain
# IMPORTANT: Make sure these domains exactly match what your frontend is using
# Include both HTTPS and HTTP versions if necessary
CORS_ALLOWED_ORIGIN=https://climasys.entrsolutions.com,https://app.climasys.entrsolutions.com,http://localhost:5173,http://climasys.entrsolutions.com

# Upload directory - setting absolute path for production environment - UPDATED for API domain
UPLOAD_BASE_DIR=/home/entrsolu/api.climasys.entrsolutions.com

# Debug level for file operations
FILE_DEBUG=true

# Email Configuration - UNCOMMENT AND CONFIGURE THESE SETTINGS FOR EMAIL VERIFICATION TO WORK
# --------------------------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com        # Your SMTP server address (e.g., smtp.gmail.com, smtp.office365.com)
# SMTP_PORT=587                   # SMTP port (usually 587 for TLS or 465 for SSL)
# SMTP_USER=your-email@gmail.com  # Your email address or username
# SMTP_PASS=your-password-or-app-password  # Your password or app password
# SMTP_SECURE=false               # Use 'true' for port 465, 'false' for other ports
# SMTP_FROM=noreply@medisync.com  # The email address that will appear in the "from" field

# Note for Gmail users:
# 1. You may need to enable "Less secure app access" or
# 2. Create an "App Password" at https://myaccount.google.com/apppasswords
# 3. For Outlook/Office365, you might need to enable SMTP AUTH in your account settings

# Note for production:
# 1. Make sure your SMTP provider allows sending from the SMTP_FROM address
# 2. Test your SMTP settings before going live
# 3. Consider using environment-specific credentials
