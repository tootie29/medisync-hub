
// Import from main utils to maintain consistency
import { getBMICategory as getBMICategoryMain, getBMICategoryColor as getBMICategoryColorMain } from '@/utils/helpers';

export const getBMICategory = getBMICategoryMain;
export const getBMICategoryColor = getBMICategoryColorMain;
